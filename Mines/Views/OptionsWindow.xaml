<Window x:Class="Mines.Views.OptionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mines.Views"
        mc:Ignorable="d"
        Title="OptionsWindow" Height="300" Width="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="4">
            <Label Content="Map Width" Margin="10,0,10,0" />
            <TextBox Name="MapWidthTb" Height="20" Width="100" Margin="10,0,10,0" />
            <Button Content="Save" Width="50" Margin="10,0,10,0" />
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="4">
            <Label Content="Map Heigth" Margin="10,0,10,0" />
            <TextBox Name="MapHeigthTb" Height="20" Width="100" Margin="10,0,10,0" />
            <Button Content="Save" Width="50" Margin="10,0,10,0" />
        </StackPanel>
    </Grid>
</Window>
