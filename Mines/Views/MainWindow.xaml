<Window x:Class="Mines.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Mines"
        mc:Ignorable="d" x:Name="mainWindow"
        Title="MainWindow" Height="350" Width="525">

    <Grid>
        <ItemsControl ItemsSource="{Binding MapModel.Fields}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <Canvas />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.ItemContainerStyle>
                <Style TargetType="ContentPresenter">
                    <Setter Property="Canvas.Left" Value="{Binding Path=XCoor}" />
                    <Setter Property="Canvas.Top" Value="{Binding Path=YCoor}" />
                </Style>
            </ItemsControl.ItemContainerStyle>
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Button Width="{Binding Path=FieldSize}" Height="{Binding Path=FieldSize}" Command="{Binding ElementName=mainWindow, Path=DataContext.FieldClickCommand}" CommandParameter="{Binding}" Tag="{Binding}" />
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>


    </Grid>


</Window>
